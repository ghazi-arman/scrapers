{
  "name": "samsclub-product-scraper",
  "version": "1.0.0",
  "description": "Scrapes Sam's Club product pages for ingredients, nutrition, images, and UPCs",
  "type": "module",
  "scripts": {
    "scrape": "npx tsx scrape.ts --config ./config.json",
    "scrape:url": "npx tsx scrape.ts --url",
    "scrape:local": "npx tsx scrape.ts --config ./config.json --local",
    "scrape:local:limit": "npx tsx scrape.ts --config ./config.json --local --limit 5"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/client-s3": "^3.0.0",
    "@aws-sdk/client-ssm": "^3.0.0",
    "@aws-sdk/lib-dynamodb": "^3.0.0",
    "axios": "^1.6.0",
    "cheerio": "^1.0.0-rc.12",
    "dotenv": "^17.3.1",
    "p-limit": "^4.0.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/uuid": "^9.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.4"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
